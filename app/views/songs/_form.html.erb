<%= form_for @song  do |f| %>
  <%= f.label :artist_name %>
  <%= artist_select(@artist_id, @song) %><br>
  <%= f.label :title %>
  <%= f.text_field :title %><br>

  <br />

  <%= f.submit %>
<% end %>
